V2.1 单路舵机投掷模块更新：

1. 主页面最下方新增单路舵机控制区。
2. 锁止按钮发送 CMD:<id>:SERVO:LOCK，无人机映射为 ESP32 串口 A30。
3. 释放按钮发送 CMD:<id>:SERVO:RELEASE，无人机映射为 ESP32 串口 A90。
4. 手动控制只允许在未解锁、任务未开始时使用；飞行中由无人机 FSM 自动投放。
5. SERVO 命令使用更长的可靠等待窗口，以覆盖 ESP32 串口重试时间。
6. ACK 后显示 A30 已锁止 / A90 已释放，超时显示错误。
